# This module was moved to `src.prompts.prompt_template`.
# Top-level wrapper removed per repository reorganization.
# Import from the new package instead:
#   from src.prompts.prompt_template import PromptBuilder

raise ImportError(
    "src.prompt_template has been removed. Import PromptBuilder from 'src.prompts.prompt_template' instead."
)
